package com.tealcube.minecraft.bukkit.mythicdrops.api.settings.replacement.language.traits

/**
 * Represents a message section with a `success` and a `failure` field.
 */
interface Feasible {
    val success: String
    val failure: String
}
