# repairing.yml

MythicDrops has a lot of configuration options. Below is the shortened contents of the repairing.yml with inline
explanations of what each configuration option does.

```yaml
version: 6.0.0
## Should anvil sounds be played when an item is repaired?
playSounds: true
```
